# Configuration de l'API Backend
# Cette variable définit l'URL de base pour toutes les requêtes HTTP vers le backend
# En développement : http://localhost:8000
# En production : remplacer par l'URL réelle du serveur backend
VITE_API_BASE_URL=http://localhost:8000

# Configuration de l'environnement
# Options : development | production | test
VITE_APP_ENV=development

# Titre de l'application affiché dans le navigateur
VITE_APP_TITLE=Ithaka - Carnets de Voyage

# Mode de debug (affiche des logs supplémentaires en console)
# true = activé, false = désactivé
VITE_DEBUG_MODE=true
